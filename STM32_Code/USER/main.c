#include "delay.h"
#include "sys.h"
#include "usart.h"
#include "pwm.h"
#include "board.h"
#include "app_servo.h"

/* ============================================================
 * ???:STM32 ??????
 *   ??:????????,?? OpenMV ? '1' ????
 *   OpenMV ??:?????????(TB6612)??????PID
 * ============================================================ */

int main(void)
{
    /* 1. ????? */
    delay_init();
    NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2);

    /* 2. ????? (9600bps, ?? OpenMV ??) */
    uart_init(9600);

    /* 3. PWM ??? (20ms ??) */
    TIM2_PWM_Init(PWM_ARR, PWM_PSC);   // ??? - PA1 (TIM2_CH2)
    TIM3_PWM_Init(PWM_ARR, PWM_PSC);   // ???? - PB5 (TIM3_CH2)

    /* 4. ????:?????,???? */
    Servo_ClawOpen();
    Servo_ArmTo0();

    /* 5. ??? - ?????? */
    while (1) {
        /* ????? LED ??????? */
    }
}
